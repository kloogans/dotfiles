#!/bin/bash
CONTROL="$HOME/.config/hyprwhspr/recording_control"
STATE="$HOME/.config/hyprwhspr/recording_status"

if [ -f "$STATE" ] && [ "$(cat "$STATE" 2>/dev/null)" = "true" ]; then
    echo stop > "$CONTROL"
else
    echo start > "$CONTROL"
fi
